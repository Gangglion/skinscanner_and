<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Skinscanner_and" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textViewStyle">@style/DefaultText</item>
    </style>

    <style name="Theme.Skinscanner_and" parent="Base.Theme.Skinscanner_and"/>

    <style name="Theme.Splash" parent="Theme.SplashScreen">
        <!-- SplashScreen의 Background 색상.-->
        <item name="windowSplashScreenBackground">@color/white</item>\
        <!-- SplashScreen에서 사용 될 아이콘. 설정하지 않으면 앱 아이콘이 사용된다. -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Skinscanner_and</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
    </style>
</resources>